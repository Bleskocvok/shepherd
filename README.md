# Shepherd
Discord BOT for scheduling exercise.

## Running the bot

```bash
python3 run.py
```

# Docker

```bash
docker build -t shepherd .
docker run shepherd
```

## Usage

`!help`
`!schedule XX:XX`
`!cancel`
`!status`
`!did N`
`!stats`
`!stats NAME`
`!allstats`
